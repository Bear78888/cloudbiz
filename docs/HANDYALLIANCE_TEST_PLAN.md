# HandyAlliance — План тестирования

Статус: живой документ. Покрывает Этапы 1–2; расширяется с каждым этапом.
Основание: `docs/MASTER_SPEC.md` §37, §38, §39.

## 1. Уровни

| Уровень | Где | Что покрывает | Как гонять |
|---|---|---|---|
| Unit (§37.1) | `tests/unit/*.test.ts` (vitest) | Чистая логика без I/O: entitlements, ядро Stripe-webhook, схема env, паритет словарей, реестры и представления Job Tracker, валидация формы, таймзоны, поиск, счётчики, CSV | `npm test` |
| Типы | весь `src/` | Паритет ключей en/es (§9.4) на уровне типа `Dict`; реестры статусов | `npm run typecheck` |
| Security / RLS (§37.4) | `supabase/tests/rls_isolation_test.sql` | Межорганизационная изоляция всех tenant-таблиц, роли, неизменяемость журналов | `psql -f` по README |
| Сборка | CI job `checks` | Сборка без env (публичный сайт) и с синтетическим env (платформа) | `npm run build` |
| E2E (§37.3) | `tests/e2e/*.spec.ts` (Playwright) | Реальный браузер против реального Next.js и реальной Supabase: регистрация → onboarding → создание работы → смена статуса; поиск, массовая смена, soft delete/restore; мобильные карточки §13.9 | `npm run test:e2e` |
| Сквозной сценарий на preview | вручную, перед merge | Развёрнутый preview с живой базой | см. §4 |

Локальный прогон миграций и RLS-тестов — раздел Database в `README.md`.
CI повторяет ту же последовательность в job `rls`.

## 2. Что покрыто unit-тестами

| Файл | Проверяет |
|---|---|
| `entitlements.test.ts` | Резолюция прав из подписок, dunning-окно, bundle → пять инструментов |
| `billing-webhook.test.ts` | Идемпотентность, повторные и «протухшие» события, несовпадение суммы |
| `env-schema.test.ts` | Слои browser ⊂ platform ⊂ integrations, префиксы ключей, сообщения без значений |
| `i18n-parity.test.ts` | Одинаковая структура ключей en/es, отсутствие пустых строк |
| `jobs-model.test.ts` | Реестр статусов §13.6 пиннится к словарям в обе стороны; каждый статус достижим из представления; «Unpaid» на восьми комбинациях; дедупликация клиентов; парсинг сумм; маржа |
| `jobs-form.test.ts` | Валидация §13.5: обязательные поля, все ошибки сразу, коды ошибок существуют в словаре, отказ от значений вне реестра, consent только по явной галочке, порядок расписания, DST по обе стороны перехода |
| `jobs-query.test.ts` | Экранирование поиска (`%`, `_`, `\`, запятая, кавычка, скобка), сборка PostgREST-фильтра, счётчики дашборда, round-trip URL-состояния списка |
| `estimates-model.test.ts` | Реестр статусов §16.8; из `draft` нет ребра в `sent`; ответ клиента необратим; арифметика с округлением до центов и скидкой, уменьшающей налоговую базу; отображение в статусы работы §16.11 (отклонение **не** делает работу `lost`); что мешает отправке |
| `estimates-schema.test.ts` | Разбор сумм (в отличие от формы работ принимает минус), ставка налога как процент с отказом от `825`, знак скидки не зависит от того, ввели ли минус, пустые запасные строки отбрасываются, а строка с ценой и без описания — ошибка |
| `estimates-public-link.test.ts` | Форма токена (43 символа base64url; padding, `/`, `+`, traversal и SQL-подобное отвергаются до запроса); что посетителю можно открыть — черновик и утверждённое-неотправленное недоступны, отвеченное читается, отозванное и просроченное закрыты, граница срока принадлежит прошлому; **каждый статус решён явно**; срок 30 дней через границу месяца и високосный день; белый список колонок не содержит ни одного поля из `FORBIDDEN_PUBLIC_FIELDS`, не содержит `*`, не содержит join'ов и не отдаёт токен |
| `email-estimate-template.test.ts` | Тема и текст называют бизнес и работу; ссылка есть и в HTML, и в plain text; plain text самодостаточен и не содержит `<`; язык — язык сметы; клиент без имени не получает «Hi null»; экранирование имени бизнеса, названия работы, имени клиента и выход из атрибута `href`; plain text **не** экранируется; ни пикселя, ни внешних адресов кроме ссылки на смету |
| `estimate-pdf.test.ts` | PDF настоящий (`%PDF-`, размер осмысленный); имя бизнеса, позиции и суммы присутствуют — проверено раскодированием сжатых (Flate) content stream через `node:zlib` и hex-строк `Tj`/`TJ`, а не поиском подстроки в сырых байтах; scope/terms есть, когда заданы, и отсутствуют (включая сам заголовок раздела), когда нет; язык — español-заголовки и English-заголовки не смешиваются, каждый в своей версии; заголовки сверяются в ЗАГЛАВНЫХ — так их рисует `textTransform: uppercase`, а не в исходном регистре словаря; ноль позиций, нулевые суммы, отсутствующие даты и 60 позиций — без исключения |
| `email-client.test.ts` | Против настоящего HTTP-сервера, а не мока `fetch`: id из ответа, заголовок и тело в форме провайдера, `reply_to` опускается, а не шлётся null; 422/401 — не повторять, 429/5xx/сеть — повторить; принято без id = ошибка (иначе вебхук §17.10 никогда не найдёт сообщение); текст ошибки обрезается, а не кладёт в базу страницу провайдера |
| `ai-estimate-draft.test.ts` | **Ни одного вызова модели — только фикстуры.** Чистый ответ, JSON в заборе и с преамбулой, округление до центов; отказ распознаётся как отказ, проза и пустое — как «нет черновика»; правдоподобный объект с `confidence: 87`, ценой −5, нулевым количеством или чужим типом строки отвергается целиком; отсутствие `confidence` — ошибка, а не догадка; черновик со словами «I can't» в допущении остаётся черновиком. Плюс §27.3: в системном промпте нет ничего, что ввёл человек; инъекция доезжает **как текст** внутри одного `<job_description>`; закрывающий тег из ввода вырезан; §16.7 — язык документа и запрет смешивать |
| `ai-transcribe.test.ts` | Против настоящего HTTP-сервера, та же причина, что у `email-client.test.ts`. Сервер сам разбирает multipart-тело (только то, что нужно этому клиенту: `model`, `language`, файл) и проверяет, что ключ, модель, язык и байты аудио доехали как есть; язык опускается, когда не задан, — провайдер сам определит. Цена считается по `duration`, которую вернул сам провайдер, а не по размеру файла; незнакомая модель или отсутствующая длительность — `provider_cost: null`. 400/401 — не повторять, 429/5xx/сеть — повторить; пустой транскрипт (тишина) — «нет текста», не ошибка, требующая повтора, но расход всё равно записан |
| `auth-email-templates.test.ts` | Supabase-рендеринга здесь нет — Node не исполняет Go-шаблоны напрямую (хотя реальный `html/template` проверен отдельно, вне vitest, против настоящего файла с диска — см. `auth-magic-link.spec.ts` ниже и §5i). Проверяется то, что ломается молча: `with .Data`/`if`/`else`/`end` сбалансированы ровно по разу (два `end`: один закрывает `with`, второй — `if`), оба языка на месте в правильном порядке, ссылка подтверждения встречается дважды на язык, цвет кнопки — актуальный `#2554eb`, `content_path` указывает на существующие файлы. Плюс регрессия на все три настоящих сбоя: ни сырое `eq .Data.preferred_locale "es"`, ни «почини-но-недостаточно» `eq (print .Data.preferred_locale) "es"` нигде не встречаются, и общее число `{{` в каждом файле — ровно 12, не больше — третий сбой был не в логике, а в том, что поясняющий комментарий выписал защиту `with` буквально и открыл незакрытый блок в прозе; посчитанный лимит ловит любой будущий вариант той же ошибки, а не только эту конкретную фразу |
| `rate-limit.test.ts` | Лимит на вызывающего, счётчик остатка, забывание после окна; ключ клиента — левый адрес `x-forwarded-for`, затем `x-real-ip`, затем константа; пустой ключ невозможен (иначе все слились бы в одно ведро) |
| `profile-model.test.ts` | §10.2 шаги 3–5: имя услуги берётся по локали и падает на другой язык, а не оставляет пустой пункт списка; ZIP+4 сводится к пяти цифрам, а не-ZIP отвергается; телефон принимается в любой записи, но только тот, который можно набрать, и `tel:` строится, не переписывая формат владельца; ссылка на отзывы — только гугловые хосты по https (`g.page.evil.com` и `javascript:` отвергнуты); день, закрывающийся раньше открытия, не «чинится» перестановкой; «закрыты в воскресенье» и «никто не заполнял» — разные ответы; пресеты есть у каждой профессии платформы и заполнены на обоих языках; пустая зона `{zipCodes: [], cities: []}` считается незаданной |
| `profile-schema.test.ts` | Профиль сохраняется наполовину заполненным; список читается и через запятую, и по строкам, дубликаты снимаются; снятая галочка дня закрывает день, хотя браузер всё равно прислал время из своих двух полей; половина строки услуги не наследует имя следующей; услуга только по-испански сохраняется. Плюс защитное чтение уже лежащего в базе jsonb: голая строка вместо списка, `{name: "..."}`, `{name: {en: 42}}` и диапазон `17:00–08:00` отбрасываются, а не доезжают до публичной страницы |
| `website-model.test.ts` | §19: слаг совпадает с `app_private.slugify` (акценты снимаются, а не превращаются в дефисы), причина отказа называется, а не сводится к «неверно», зарезервированные адреса закрыты, регистр не влияет; публичный URL не выдумывается без базового адреса; черновик перевода снова становится непроверенным после регенерации — пара меток времени, а не флаг; пустой блок исчезает — Reviews без ссылки на отзывы, Service Area без зоны, Call Button без телефона, About/FAQ/«почему мы» без текста (§19.8), — а Hero и Footer остаются всегда; готовность к публикации не признаёт непроверенный машинный перевод, но и не держит сайт заложником черновика на языке, который больше не предлагается |
| `website-render.test.ts` | Сборка страницы §19.4: услуги разрешаются в язык страницы и падают на другой язык, услуга без имени вообще выбрасывается; пустой заголовок падает на **название бизнеса**, а не на придуманный слоган (§19.8); переключатель языка предлагает только те языки, на которых сайт правда предлагается; у бизнеса без данных остаются ровно `hero`, `contact_form`, `footer`; зона обслуживания печатается как введена, без «и окрестности». Плюс тема: палитра есть у каждого утверждённого пресета, класс нигде не собирается строкой (Tailwind сканирует исходник — интерполированный класс существует в коде и отсутствует в стилях, и страница отрендерилась бы бесцветной молча), неизвестный пресет падает на дефолт вместо неоформленной страницы |
| `website-schema.test.ts` | Две формы §19.3: адрес приводится к нижнему регистру, причина ошибки слага доезжает до самого поля; шаблон и цвет вне утверждённого набора отвергаются (§19.9); языки упорядочены по реестру платформы, а не по форме, и чужая локаль игнорируется; выключенные блоки выводятся из реестра, а не из сабмита — несуществующий блок ничего не выключает, галерея и Hero/Footer никогда не попадают в исключения, а пустой сабмит означает «все выключены»; контент сохраняется наполовину написанным, строки «почему мы» читаются по одной в строке, а половина FAQ-строки — ошибка, а не вопрос без ответа на публичной странице |
| `jobs-csv.test.ts` | Разбор CSV (три разделителя, кавычки, переносы внутри поля, BOM, CRLF, короткие строки), автоопределение колонок en/es, даты US и ISO, статусы-подписи вместо кодов, дубликаты в файле и против базы, экранирование экспорта и защита от формул |

## 3. RLS-сценарии (`rls_isolation_test.sql`)

Самопроверяющийся скрипт: любая непройденная проверка бросает исключение,
поэтому ненулевой код возврата psql = провал.

1. Участник видит ровно свою организацию.
2. Кросс-тенантное чтение возвращает ноль строк для всех tenant-таблиц.
3. Бесплатный entitlement `job_tracker` создаётся вместе с организацией (§13.1).
4. Прямой INSERT в `organizations` запрещён — только через RPC.
5. Чужую организацию нельзя обновить.
6. `is_member_of` для чужой организации — false.
7. Staff не видит подписки, но видит entitlements и свою организацию (§11.3).
8. `audit_logs` неизменяемы даже для владельца таблицы.
9. Работа и клиент создаются, журнал пишется автоматически с верным актором;
   смена статуса, soft delete и восстановление дают свои события;
   `phone_digits` генерируется и находит номер, записанный иначе.
10. Владелец B не видит данных A и видит свои; кросс-тенантные INSERT/UPDATE отклонены.
11. Журнал нельзя ни подделать, ни изменить, ни удалить.
12. Staff работает с Job Tracker своей организации и не видит чужую.
12b. CSV-импорт: атомарность, сопоставление клиента по телефону в другом формате,
    события `job.imported` вместо `job.created`, транзакционно-локальный контекст
    не протекает за пределы импорта, запись в audit log, отказ при кросс-тенантном
    вызове и при значении вне реестра (ни одной строки не остаётся).
13. Организация удаляется при наличии работ; история активности и аудита переживает удаление.
14. Anon видит ноль организаций, работ, клиентов и записей журнала.
15. Матрица грантов Data API совпадает с матрицей политик: у `anon` нет ни одного
    табличного гранта (15a), у `authenticated` нет глагола без политики (15b),
    и нет политики без гранта (15c). Проверка читает `information_schema`, а не
    результат запроса: при forced RLS лишний грант выглядит как пустая выборка и
    иначе не ловится.
16–19. Очередь синхронизации (§14.9, §14.11), недосягаемость refresh-токенов
    (§14.16), сметы (§16) и журнал доставки (§16.9) — сценарии есть в скрипте,
    подробности там же в комментариях.
19f–19i. Профиль бизнеса (§10.2 шаги 3–5) получает первого писателя вместе с
    сайтом, поэтому owner-only правило на нём проверяется, а не подразумевается:
    владелец заполняет свой профиль, staff его читает, но не меняет; голая
    строка вместо списка услуг отклонена jsonb-констрейнтом.
20. Business Website (§19): владелец создаёт сайт и читает его; staff читает, но
    не меняет ни шаблон, ни заголовок — то, что бизнес о себе говорит, меняет
    только владелец; чужой тенант не видит и не создаёт; шаблон вне утверждённого
    набора отклонён (§19.9); галерею нельзя записать как «скрытый блок», а
    настоящий необязательный блок выключается; контент только на языках
    платформы, а списковые колонки обязаны быть списками; `anon` не читает ни
    настройки, ни тексты — даже те, что предназначены для публики (§19.10).

## 4. Сквозной сценарий Этапа 2 (§37.3, сценарий A)

Проверяется перед каждым merge — в браузере против реального Supabase и
затем на развёрнутом preview.

1. Вход в кабинет, создание организации (если её нет).
2. Пустое состояние списка работ.
3. Создание работы: клиент, телефон, услуга, статус, источник, расписание, суммы.
   **Критерий §13.11: меньше минуты.**
4. Карточка работы: клиент, деньги, маржа после материалов, расписание в
   таймзоне организации, журнал с событием создания.
5. Валидация: сумма «abc» даёт понятную ошибку у поля, а не 500.
6. Смена статуса с карточки; в журнале появляется переход «из → в».
7. Поиск по телефону клиента, записанному в другом формате; поиск без совпадений
   даёт отфильтрованное пустое состояние.
8. Представления §13.7: работа видна в «Scheduled» и не видна в «Lost».
9. Мобильный экран (390 px): широкой таблицы нет, карточка показывает клиента и статус.
10. Испанская локаль: список и карточка на испанском.
11. Дашборд: счётчики перестали быть прочерками и ведут в нужные представления.
12. Soft delete: работа исчезает из списка, доступна под фильтром «Deleted»,
    восстанавливается; журнал содержит удаление и восстановление.
13. CSV-импорт: файл → сопоставление колонок → preview с дубликатами → подтверждение;
    работы появляются в списке, клиенты сопоставлены, а не задвоены.
14. CSV-экспорт открывается в таблице, колонки совпадают с Jobs-табом §14.7.

### Ограничение окружения

Браузер в сборочном контейнере не имеет исходящего HTTPS, поэтому клиентская
форма входа (она обращается к Supabase напрямую из браузера) в нём не работает.
Сквозной прогон выполняется так: сессия берётся через auth API, кладётся в
cookie `@supabase/ssr`, дальше всё идёт обычным путём — серверные компоненты,
server actions, живая база. Развёрнутый preview дополнительно проверяется
HTTP-запросами с той же сессией.

## 4a. E2E в CI

Job `e2e` поднимает на раннере локальный стек Supabase (`supabase start`:
Postgres + API + auth), применяет к нему миграции из репозитория, собирает
приложение и гоняет Playwright. Секретов по-прежнему нет: ключи локального
стека — фиксированные dev-значения, которые печатает `supabase status`, и
берутся оттуда, а не из GitHub Secrets.

Что покрывают спеки:

| Файл | Сценарий |
|---|---|
| `job-tracker.spec.ts` | Регистрация → onboarding → создание работы → смена статуса, с проверкой таймзоны, сумм и записи в журнал; поиск по телефону в другом формате и отфильтрованное пустое состояние; массовая смена статуса; soft delete и восстановление; понятная ошибка валидации вместо тишины |
| `sheet-sync.spec.ts` | **Инвариант: каждое событие в `synced` имеет строку в таблице.** Google подменён локальной заглушкой (`sheets-stub.mjs`), потому что дефект был в нашей бухгалтерии, а не в Google, и посчитать пришедшее можно только так. Плюс проверка поля с внешней зависимостью: ссылка в строке начинается с адреса развёртывания, а не с константы |
| `estimates.spec.ts` | **Ворота утверждения §16.5:** пустой черновик утвердить нельзя (отказ — и есть проверка); после заполнения строк итог на экране совпадает с сохранённым; утверждение открывает отправку, отправленную смету редактор больше не показывает (§25.3); работа идёт следом за сметой (§16.11), принятие пишет сумму в работу; вторая смета — версия v2, а не правка v1. Отдельно: правка утверждённой сметы возвращает её в черновик, и «Отправить» исчезает |
| `public-estimate.spec.ts` | **Первая внешняя поверхность.** Клиент — отдельный browser context без сессии: открывает ссылку, видит смету и не видит телефон/email клиента, слова «Job Tracker»/«Sign In», ссылок в `/app/`; `noindex` на месте; принимает — работа следует за сметой (§16.11), повторный ответ невозможен. Отдельно: подделанный токен той же длины и мусор (`../../etc/passwd`, SQL-подобное) дают 404; отзыв сметы убивает ссылку (200 → 404), но у владельца смета остаётся; отклонение оставляет смету читаемой и не роняет работу в `lost` |
| `estimate-pdf.spec.ts` | Владелец получает настоящий PDF (`Content-Type`, `%PDF-` в начале тела) для собственной сметы в любом статусе — статус-гейта на этом маршруте нет, потому что посмотреть документ до Approve — не то, что запрещает §16.5; чужая (неавторизованная) попытка получает тот же 404, что и сама страница сметы при отсутствии членства. Клиент по публичной ссылке получает тот же PDF; подделанный токен даёт 404 — тот же `getEstimateByToken`, что и у HTML-страницы, а не вторая копия правил §16.8 |
| `estimate-email.spec.ts` | **Инвариант: «Отправлено» на экране = сообщение в outbox.** Resend подменён локальной заглушкой (`resend-stub.mjs`), потому что проверять баннер бессмысленно — он бы прошёл и при нулевой отправке. Проверяется адрес, язык, сумма и **ссылка из письма реально открывает смету** (поле с внешней зависимостью). Отдельно: до Approve кнопки отправки нет и письма нет (§27.4); клиент без email получает объяснение, а не тишину, и смета остаётся `Approved`; отказ провайдера (заглушка `fail-next`) оставляет смету неотправленной, и повтор срабатывает |
| `estimate-ai.spec.ts` | Модель подменена заглушкой (`ai-stub.mjs`): реальный вызов был бы медленным, платным и по-разному неверным каждый прогон. Проверяется, что черновик попадает **в `draft`** и кнопки отправки нет — §16.5 не отменяется генерацией; что уверенность видна до Approve и низкая помечается, а не прячется; **§27.3 сквозь весь стек** — инъекция из описания приходит в `user` внутри делимитера, а не в `system`; отказ модели не меняет ничего (своя строка на месте); пустое описание не доходит до модели вовсе. `afterEach` возвращает заглушку к дефолтному ответу — она один процесс на весь прогон, и «низкая уверенность» здесь меняет его не для одного своего теста, а до следующего `POST /__stub/reply` вообще, найдено на CI, когда `estimate-voice.spec.ts` унаследовал чужой ответ («Investigate» вместо «Installation, 3 hours») |
| `estimate-voice.spec.ts` | Провайдер речь-в-текст подменён заглушкой (`transcribe-stub.mjs`), та же причина, что у `estimate-ai.spec.ts`. Загрузка файла в скрытый `#ai_voice_note` (`page.setInputFiles`, без клика) кладёт транскрипт в тот же `#ai_description`, что и рукописный текст, — редактируемым, не сразу в черновик; текст, напечатанный до записи, дополняется, а не стирается. Отказ провайдера оставляет описание пустым (не половиной чужого текста); слишком большой файл отклоняется на клиенте до какой-либо отправки (заглушка не получает запроса). Чужой сигнатуре запроса к `.../transcribe` без сессии не добраться — маршрут под `/app/`, тот же `middleware.ts`-редирект на `/sign-in`, что и у `estimate-pdf.spec.ts`; отправленная смета не показывает поле записи вовсе, тем же гейтом `isEditable`, что и у формы AI-черновика |
| `auth-magic-link.spec.ts` | Запрос ссылки для входа проходит на английской и испанской страницах входа. **Четыре красных прогона подряд под одним и тем же generic-баннером — из них три диагноза оказались реальными, но не тем, что держало CI красным.** (1) Прямое сравнение `eq .Data.preferred_locale "es"` роняло `signInWithOtp` целиком — реальный баг. (2) Починка `print(...)` не помогла; собственный трейс Playwright (`trace: "retain-on-failure"`) дал настоящий ответ `/auth/v1/otp` (`{"code":"unexpected_failure",...}`, HTTP 500), а воспроизведение того же `.Data`-шаблона через настоящий `html/template` показало реальную причину: `nil pointer evaluating interface {}.preferred_locale`, паника при чтении поля до `print`/`eq` — тоже реальный баг, `with .Data` — верная починка. (3) Эта починка сама сломала парсинг: поясняющий комментарий выписал синтаксис защиты буквально, открыв незакрытый блок в прозе — тоже реальный баг, найден только повторным исполнением настоящего файла с диска. **(4) После всех трёх починок CI упал в четвёртый раз с тем же самым ответом байт-в-байт — и это раскрыло, что ни один из трёх багов не был причиной хоть одного красного прогона.** `internal/api/mail.go` GoTrue заворачивает parse-, execute- и delivery-ошибки в один и тот же текст; полный дамп лога контейнера (не `--tail`, который дважды не успевал за трафиком сьюта) и его переменных окружения показал настоящую причину: `dial tcp: lookup supabase_inbucket_cloudbiz ...: server misbehaving` — DNS-сбой, потому что `mailpit` был исключён из `supabase start -x ...` ради скорости сборки, решение, принятое до появления в сьюте хоть одного теста, реально пытающегося отправить письмо. Без него GoTrue физически не может отправить ничего, вне зависимости от корректности шаблона — а `content_path`-загрузка кастомного шаблона в этом стеке тоже не работает отдельно (Kong не отвечает на порту 8088), так что наш HTML в CI ни разу не исполнялся по-настоящему; GoTrue тихо использовал свой дефолт. `mailpit` возвращён в CI-стек — это и есть настоящая починка. Само содержимое письма по-прежнему не читается (Mailpit не подключён к тесту, только к GoTrue) — тест подтверждает «запрос не падает на обоих языках», не «испанский текст показывает испанское письмо», и не может подтвердить это в текущем виде стека даже теоретически |
| `business-site-public.spec.ts` | **Правило §19 про бренд, проверяемое только на отрендеренной странице.** Посетитель — отдельный browser context без сессии. Проверяется: preview показывает то же, что опубликуется (тот же `buildRenderableSite` + `SiteView`), до Publish `/pro/{slug}/en` отвечает 404, после Publish страница открывается и на ней **нет** слов «handyalliance», «job tracker», «sign in», «all tools», ни одной ссылки в `/app/`, `/sign-in` или `/pricing` — та же ошибка уже случалась на публичной смете и ловится тем же способом. Плюс §19.8: блока Reviews нет, потому что ссылки на отзывы не давали; зона обслуживания напечатана ровно как введена. В отличие от сметы, `noindex` здесь **не** ставится — эту страницу положено находить. «Снять с публикации» гасит адрес (404), контент остаётся. Отдельно: незаполненный сайт публиковать нельзя (кнопка disabled), а чужой слаг, неопубликованный слаг и голый `/pro/{slug}` дают один и тот же 404 — иначе посетитель отличил бы «бизнес есть, но скрыт» от «такого нет» |
| `business-profile.spec.ts` | **Замыкание петли, которое unit-тест показать не может.** До этого экрана `business_profiles` не писал никто, и чек-лист готовности сайта просил телефон, которому неоткуда было взяться. Тест заполняет профиль (телефон, почта, ZIP-ы с дубликатом, город, пресет услуги с последующим переименованием, часы понедельника), перезагружает — всё возвращается, ZIP-ы дедуплицированы, — и **идёт на экран сайта**: блокеры «добавьте телефон» и «добавьте услуги» исчезли, а секции Services / Service area / Call button появились. Reviews по-прежнему нет: ссылку на отзывы не давали, а выдумывать §19.8 запрещает. Отдельно: телефон, который нельзя набрать, и не-гугловая ссылка на отзывы отклоняются у поля, а не роняют страницу |
| `business-website.spec.ts` | Настройки сайта §19.3–19.5 против живой базы. Проверяется ровно то, что нельзя проверить юнит-тестом, потому что за это отвечают страница и база одновременно: адрес уникален на всю платформу — второй бизнес, вводя занятый слаг, получает предложение у поля, а не имя констрейнта (§29); **и §19.10 — сохранённое здесь никуда не публикуется**: `/pro/{slug}/en` отвечает 404 (`maxRedirects: 0`, иначе тест увидел бы страницу редиректа, а не код маршрута). Плюс: адрес предлагается из названия бизнеса; список «что осталось сделать» уменьшается по мере заполнения; две формы независимы (сохранение текста не трогает адрес); пустой блок не показывается — Reviews и Service Area отсутствуют у нового бизнеса, Photos отсутствуют всегда, — а выключенный вручную блок остаётся выключенным после перезагрузки |
| `mobile.spec.ts` | Viewport Pixel 7: широкой таблицы нет, карточка показывает клиента/работу/статус/сумму, нет горизонтального переполнения, тап открывает работу; испанская локаль на телефоне |

Оба макета §13.9 присутствуют в DOM одновременно (один скрыт CSS'ом), поэтому
спеки работают только с видимым: `input[name="job_ids"]:visible`.

## 5. Ручные проверки перед merge (§39)

- [ ] English UI и Spanish UI на месте, ключи не разъехались (typecheck + `i18n-parity`).
- [ ] loading / empty / error / not-found состояния видны глазами.
- [ ] Мобильный экран проверен на 390 px.
- [ ] Клавиатурная навигация: у полей есть подписи, ошибки помечены `role="alert"`,
      цвет не единственный носитель смысла (§8.3).
- [ ] Preview Deployment открыт и ключевой сценарий пройден.
- [ ] В диффе нет секретов.
- [ ] Миграция применена к живой базе и проверена `get_advisors`.

16. Outbox (§14.9, §14.11): создание работы ставит событие; три быстрые правки
    дают **одно** событие, а не три (debounce); правка, ничего не меняющая,
    не ставит событие и не будит уже отправленное; правка после `synced`
    ставит новое, не затирая отправленное; soft delete остаётся `upsert`
    (§14.12 — строка помечается Deleted, а не исчезает из таблицы); чужую
    очередь организация не видит.
17. `google_oauth_tokens` недоступна ни одной клиентской роли (§14.16):
    отказ на слое грантов, а не пустая выборка.
19. Журнал доставки `outbound_emails`: организация читает свой и не видит чужой,
    клиент не может записать свою строку (нет ни политики, ни гранта на insert),
    `sent` без provider id отклоняется ограничением. 19c и 19d прогнаны с
    негативным контролем в изоляции — с грантом и политикой 19c падает, без них
    проходит; 19d падает на `outbound_emails_sent_snapshot`. (В полной сюите 19c
    опережает 15b, как 18j опережает 15a.)
18j–18k. `anon` не может получить смету по токену и не может читать строки смет:
    отказ на слое грантов, а не пустая выборка. Прогнано с негативным контролем в
    изоляции: с грантом проверка бросает `FAIL(18j)`, без гранта проходит.
    (В полной сюите её опережает 15a — матрица грантов ловит лишний грант раньше
    и точнее; 18j остаётся как проверка поведения, а не конфигурации.)
18. Сметы (§16): организация B не видит ни сметы A, ни её строк и не может
    вставить смету в A; смету нельзя удалить (документ, на который ссылаются),
    а строку сметы — можно (обычная правка черновика); ставка налога `8.25`
    отклоняется, `0.0825` принимается (18f–18g); статус `sent` без
    `public_token` отклоняется (18h), `accepted` без `accepted_at` — тоже (18i).
    Три последние проверки прогнаны с негативным контролем: каждая падает
    именно на своём ограничении (`estimates_tax_rate_check`,
    `estimates_sent_has_token`, `estimates_accepted_snapshot`), а легальные
    значения проходят.

## 5a. Перед запуском

Проверки, привязанные к домену и живым ключам, вынесены в
`docs/HANDYALLIANCE_LAUNCH_CHECKLIST.md` — их нельзя пройти на
`*.vercel.app`, а часть (проверка Google consent screen, свой SMTP) требует
недель, а не минут, и начинается заранее.

Отдельно оттуда, потому что это не «настройка на потом», а **блокер пилота**:
встроенный мейлер Supabase отдаёт единицы писем в час, и тест регистрации
2026-08-04 упёрся в `over_email_send_rate_limit`. До первых живых
пользователей нужен свой SMTP через Resend.

## 6. Чего пока нет

- Integration-тесты API (§37.2) поверх реального Supabase: появятся вместе с
  Google Sheets Sync, где без них не обойтись.
- Нагрузочные проверки (§30.1) — на этапе pilot readiness (§35, Этап 11).
